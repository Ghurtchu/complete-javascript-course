const a = 5
var job = 'brogrammer'
job = job.replace('b', 'p')

2 ** 3

console.log((now - 1991) > (now - 2018))

function fibo(n) {
    if (n <= 1) return 1
    else return fibo(n - 1) + fibo(n - 2)
}

let x, y // undefined
x = y = (10 + 25)
x == y // true
x === y // true

const stringLiterals = "I'm Jonas"
const str = 'I\'m Jonas'

let d = 5

function d() {
    return 5
}

// template strings, template literals
const wow = `I'm someone ${d()}`

// multiline strings
let oldMultiline = "hello there\n\ second line\n\ third line"

let newmutliline = `hello
what's up?
oh yeah, sure.
no.`